"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Logger = void 0;
const Verbosity_1 = require("./Verbosity");
class Logger {
    constructor(minimumVerbosity) {
        this.MinimumVerbosity = minimumVerbosity;
    }
    /*
     * Writes a message to standard output followed by a line break.
     *
     * @param message Message to write.
     * @param verbosity Verbosity of the message.
     */
    Write(message, verbosity) {
        if (verbosity >= this.MinimumVerbosity && verbosity !== Verbosity_1.Verbosity.Silent) {
            process.stdout.write(`${message}\n`);
        }
    }
    /*
     * Writes a message to standard error followed by a line break.
     *
     * @param message Message to write.
     */
    WriteError(message) {
        process.stderr.write(`${message}\n`);
    }
}
exports.Logger = Logger;
